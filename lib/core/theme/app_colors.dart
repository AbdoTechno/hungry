import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Colors
  static const Color primary = Color(0xFF0B5D1E);
  static const Color primaryDark = Color(0xFF084318);
  static const Color primaryLight = Color(0xFF1B7A33);

  // Background Colors
  static const Color background = Color(0xFFF8F8F8);
  static const Color darkBackground = Color(0xFF121212);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Text Colors
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF7A7A7A);
  static const Color textWhite = Color(0xFFFFFFFF);

  // Accent Colors
  static const Color accent = Color(0xFFFFC107);
  static const Color success = Color(0xFF22C55E);
  static const Color error = Color(0xFFEF4444);

  // UI Colors
  static const Color border = Color(0xFFE5E5E5);
  static const Color divider = Color(0xFFF0F0F0);
  static const Color shadow = Color(0x14000000);

  // Buttons
  static const Color buttonPrimary = primary;
  static const Color buttonSecondary = Color(0xFFF3F4F6);

  // Inputs
  static const Color inputFill = Color(0xFFF5F5F5);
  static const Color inputBorder = Color(0xFFE0E0E0);

  // Cart / Price
  static const Color price = Color(0xFF0B5D1E);

  // Bottom Navigation
  static const Color navActive = primary;
  static const Color navInactive = Color(0xFF9CA3AF);
}